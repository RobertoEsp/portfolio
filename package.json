{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tail": "npx tailwindcss -i ./src/input.css -o ./src/output.css --watch",
    "live": "live-server src --port=3030 --no-browser --wait=100",
    "dev": "concurrently \"npm run tail\" \"npm run live\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "tailwindcss": "^3.4.6"
  }
}
